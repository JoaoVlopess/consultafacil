import { Metadata } from "next";

export const metadata: Metadata = {
  title: 'Dashboard'
}

const Page = () => {
  return (
    <div className="min-h-screen flex">
        olá
    </div>
  )
}

export default Page;